<header class="my-6">
    <h1 class="title has-text-weight-light is-size-1">{{ $params['title'] }}</h1>

    @if ($params['subtitle'])
        <h2 class="subtitle has-text-weight-light">{{ $params['subtitle'] }}</h2>
    @endif
</header>